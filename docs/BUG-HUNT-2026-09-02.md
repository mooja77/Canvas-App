# Bug hunt 2026-09-02 (main @ 97458e1)

**Status 2026-09-03:** every High, Medium and Low item below except L12 and L14 is fixed on
`fix/bug-hunt-2026-09-02`, each with a regression test that failed before the change. L12 (CSV header
heuristics) and L14 (apostrophe-prefixed dash cells) are deliberate trade-offs left as documented.

Four execution-verified sweeps over the code shipped 26-29 Aug 2026 plus the parser surface. Every
finding below was demonstrated by a failing test, a measured timing, or a real-Postgres repro
(local qc-e2e-pg, all 39 migrations). "Unproven" items were suspected but not demonstrated.

## High

| #   | Area         | File                                                   | Finding                                                                                                                                                                                                                                         | Fix                                                                          |
| --- | ------------ | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- |
| H1  | Artifacts    | `apps/frontend/src/hooks/useCanvasArtifact.ts:118-144` | A slow/failed GET returns the mount-time `localValue`; an edit made meanwhile vanishes from state, and the next edit overwrites localStorage + server without it. Repro: state `[]` after GET rejection, final PUT `["note-D"]` (expected C+D). | On failure re-read the store; skip `setValue(next)` when edited since mount. |
| H2  | Artifacts    | same, `:102-111`; `canvasRoutes.ts:329-355`            | Edit before GET resolves marks dirty, then PUTs local `[C]` over server `[A,B]`. Unconditional upsert, no version check.                                                                                                                        | Merge by id; `updatedAt` precondition on PUT.                                |
| H3  | Canvas store | `apps/frontend/src/stores/canvasStore.ts:240-299`      | No request-generation guard: slower earlier `openCanvas`/`refreshCanvas` overwrites the chosen canvas. Worst case `activeCanvasId=B` with `activeCanvas` = A (refreshCanvas fires on every socket connect).                                     | `loadSeq` counter; only `set` if still current.                              |
| H4  | QDPX export  | `apps/backend/src/utils/qdpxExport.ts:19-35`           | A parent cycle (A->B->A) silently drops both codes from the archive; codings dangle.                                                                                                                                                            | `visited` set; treat revisited node as root.                                 |
| H5  | Text search  | `apps/backend/src/utils/textAnalysis.ts:16`            | ReDoS guard misses `?`-terminated groups: `(a+?)+$` on 28 chars = 5.36 s per exec; budget only checked between execs.                                                                                                                           | Extend check to `[+*?]\)`; hard timeout / RE2.                               |
| H6  | Survey CSV   | `apps/backend/src/utils/csvParser.ts:44`               | BOM + quoted header (Excel "CSV UTF-8", Qualtrics) fails: `Title column "id" not found`.                                                                                                                                                        | Strip leading U+FEFF.                                                        |

## Medium

| #   | Area           | File                                                                                                 | Finding                                                                                                                                                                                                     | Fix                                                                         |
| --- | -------------- | ---------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| M1  | Pagination     | `api.ts:135-160` / `canvasRoutes.ts:212-224`                                                         | Offset paging: a delete between pages drops a live row and keeps a deleted one (>500 rows).                                                                                                                 | Keyset paging or dedupe by id.                                              |
| M2  | Pagination     | `api.ts:162-181` / `canvasRoutes.ts:64`                                                              | Canvas list ordered `updatedAt desc` with offsets: duplicate + missing canvas when any is touched between pages (>200 canvases).                                                                            | Dedupe / cursor.                                                            |
| M3  | Layout save    | `canvasStore.ts:34-36,597-601`; `api.ts:35-39`                                                       | Global queue, no HTTP timeout: one hung PUT blocks every later layout save across canvases; `savingLayout` stuck.                                                                                           | Per-canvas queue, timeout, coalesce.                                        |
| M4  | Hierarchy      | `codingRoutes.ts:39-70,119-125`                                                                      | Cycle check not atomic: concurrent `PUT A{parent:B}` + `PUT B{parent:A}` both 200 (23/25 runs); afterwards every hierarchy edit 409s.                                                                       | Transaction + row lock; repair path.                                        |
| M5  | Trash job      | `apps/backend/src/jobs/canvasTrashRetention.ts:55-57`                                                | Canvas restored during the sweep is still hard-deleted (uploads already gone).                                                                                                                              | `deleteMany` with `deletedAt < cutoff`; purging marker.                     |
| M6  | Plan gating    | `planLimits.ts:66-110` vs StatusBar/AddComputedNodeMenu/CanvasToolbar/QdpxExportButton/AiSetupBanner | Server gates on canvas OWNER's plan, UI on VIEWER's. Free collaborator on Team canvas sees red caps and hidden tools the server allows; Pro collaborator on Free canvas gets 403s the UI never warns about. | Return owner's effective plan on `GET /canvas/:id`; canvas gates read that. |
| M7  | QDPX import    | `qdpxImport.ts:292`                                                                                  | `color` stored unvalidated (`=HYPERLINK(...)`), bypassing the API's `#rrggbb` rule.                                                                                                                         | Same regex, default colour.                                                 |
| M8  | Text search    | `textAnalysis.ts:266-274`                                                                            | "too complex" AppError, invalid regex and astral ranges are thrown inside `try { } catch { continue }`: user sees 0 results, never an error.                                                                | Validate before the try; 400 on compile error.                              |
| M9  | Transcript CSV | `apps/frontend/src/utils/csv.ts:37`                                                                  | Unquoted inch mark (`6" tall`) opens a quoted field and swallows all following rows (3 transcripts -> 1). Backend parser is correct.                                                                        | Only open quote at field start.                                             |
| M10 | Captions       | `apps/frontend/src/utils/subtitles.ts:36`                                                            | Cue text starting "Note"/"Style"/"Region" (case-insensitive, ignores `inCueText`) is deleted up to the next blank line.                                                                                     | Case-sensitive, only when not in cue text.                                  |
| M11 | Codebook CSV   | `codebookExportFormat.ts:53,61`                                                                      | `color`, `frequency`, offsets etc. interpolated raw (chains from M7: 12 fields instead of 10).                                                                                                              | `escapeCsvField` every column.                                              |

## Low

- L1 `codingRoutes.ts:88,119` + `validation.ts:142,155`: `parentQuestionId: ''` -> Prisma P2003 -> 500 on create and update.
- L2 `canvasRoutes.ts:208-213`: `detailPage=99999999999999999999` -> Prisma skip overflow -> 500.
- L3 `adminRoutes.ts:41-64`: `realUsersWhere` substring filter (`test|demo|qa|seed`) excludes real users (`qu.edu.qa`, `maria.testa@`, `j.seedorf@`, `contest.winner@`) from cohort/funnel/counts.
- L4 `templateRoutes.ts:209-243` PATCH /user/onboarding: no schema; stores unknown ids, arrays, `__proto__` key; client can regress completion; second device erases server ticks.
- L5 `authStore.ts:286-298`: `VITE_E2E` jwt gate is dead (migrate+merge already delete jwt); 49 e2e files read `state.jwt` which is always empty; they pass only because the cookie wins.
- L6 `public/cookie-consent.js:41-48,171-182`: Reject-after-accept reloads even if the localStorage write threw; GTM re-injects, no banner.
- L7 `shared/utils/academicEmail.ts:31`: bare `x@edu.au`, `x@ac.uk`, `x@edu.ie` count as academic; an address with a zero-width space before `.edu` passes because signup only checks `includes('@')`.
- L8 `qdpxParse.ts:230-231,271-272`: non-numeric offsets coerce to 0 and fabricate a coding; floats truncate.
- L9 `qdpxParse.ts:174,178`: `&#x110000;` throws RangeError -> 400 with raw message.
- L10 `qdpxParse.ts:600-602`: U+FFFD disclosure omits code names.
- L11 `csvParser.ts`: lone CR row separators rejected.
- L12 `transcriptFiles.ts` header heuristics: `Q1,Q2` imports junk; `Speaker,No` drops a real row; semicolon CSV -> one transcript per line.
- L13 `subtitles.ts:47,55`: prose `-->` dropped as timing line; `a < b then c > d` stripped as a tag.
- L14 `delimitedText.ts`: `-` / `-5` become `'-` / `'-5` (visible in R/pandas).
- L15 `qdpxImport.ts` ~bytes 6137/6352: two literal NUL bytes as dedupe-key separators. Behaves correctly, but git/grep treat the file as binary so diffs are hidden. Replace with the backslash-u-0000 escape sequence.

## Verified clean

QDPX round trip (CRLF, emoji, lone surrogates, U+FFFE, 100k chars, boundary/overlap/zero-length codings) is offset-exact. Migration 0037 partial unique: all rename/restore paths correct; `prisma migrate diff` empty. Artifact routes behind auth + viewerWriteGuard; IDOR/viewer-write both 403. Pagination ordering deterministic (id tiebreak). Retention selects only `deletedAt < cutoff`. activationFunnel: no NaN on empty cohorts. Pilot honeypot/zod fine. authStore v0->v1 migration strips secrets; no redirect loop. Cookie consent otherwise correct (GA cookies expired, exactly one reload). Lifecycle email links/unsubscribe/flags correct. Plan numbers identical across shared/frontend/pricing/landing. Prod: all 33 App routes resolve as intended (404 for unknown), CSP covers every host the frontend calls, all 18 training videos + 4 playlists resolve on YouTube, share expiry and public-share allow-list fixes present.

## Unproven (not demonstrated)

Edge-sync empty-recovery branch resurrecting user-removed edges; `-server-dirty` marker stuck after a quota failure; `getSocket` churning connections during handshake; honeypot 202 vs 201 fingerprint; errorHandler `headersSent` hang if a route errors mid-stream; funnel attributes collaborator codings to the owner; `/usage` sub-aggregates skip `realUsersWhere`; frontend `codeTree.ts:44` hides both codes of a 2-cycle; `code-weights` uncapped keys; leading-space `=` in CSV; foreign CRLF QDPX offset convention; Zoom/Otter cue files without blank lines; `searchTranscripts` has no result-size cap.
