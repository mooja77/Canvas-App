{
  "name": "canvas-app-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute canvas-app-db --file=schema.sql --remote"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "jose": "^5.0.0",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.0.0",
    "@types/bcryptjs": "^2.4.6"
  }
}
